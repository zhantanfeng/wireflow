apiVersion: v1
kind: Namespace
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: wireflow-controller
    control-plane: controller-manager
  name: wireflow-system
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: networkpolicies.wireflowcontroller.wireflow.run
spec:
  group: wireflowcontroller.wireflow.run
  names:
    kind: NetworkPolicy
    listKind: NetworkPolicyList
    plural: networkpolicies
    shortNames:
    - wfpolicy
    - wfp
    singular: networkpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The type of the network policy (ingress or egress)
      jsonPath: .spec.policyType
      name: TYPE
      type: string
    - description: The selector to identify nodes this policy applies to
      jsonPath: .spec.nodeSelector
      name: NODE-SELECTOR
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - description: Number of nodes targeted by this policy
      jsonPath: .status.targetNodes
      name: TARGETS
      type: integer
    - description: Number of rules defined in this policy
      jsonPath: .status.ruleCount
      name: RULES
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NetworkPolicy is the Schema for the networkpolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NetworkPolicySpec defines the desired state of NetworkPolicy.
              which used to control the wireflow's traffic flow.
            properties:
              egressRule:
                items:
                  properties:
                    ports:
                      items:
                        properties:
                          port:
                            format: int32
                            type: integer
                          protocol:
                            type: string
                        type: object
                      type: array
                    to:
                      items:
                        properties:
                          ipBlock:
                            properties:
                              cidr:
                                type: string
                            type: object
                          peerSelector:
                            description: |-
                              A label selector is a label query over a set of resources. The result of matchLabels and
                              matchExpressions are ANDed. An empty label selector matches all objects. A null
                              label selector matches no objects.
                            properties:
                              matchExpressions:
                                description: matchExpressions is a list of label selector
                                  requirements. The requirements are ANDed.
                                items:
                                  description: |-
                                    A label selector requirement is a selector that contains values, a key, and an operator that
                                    relates the key and values.
                                  properties:
                                    key:
                                      description: key is the label key that the selector
                                        applies to.
                                      type: string
                                    operator:
                                      description: |-
                                        operator represents a key's relationship to a set of values.
                                        Valid operators are In, NotIn, Exists and DoesNotExist.
                                      type: string
                                    values:
                                      description: |-
                                        values is an array of string values. If the operator is In or NotIn,
                                        the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                        the values array must be empty. This array is replaced during a strategic
                                        merge patch.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  required:
                                  - key
                                  - operator
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              matchLabels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                  map is equivalent to an element of matchExpressions, whose key field is "key", the
                                  operator is "In", and the values array contains only "value". The requirements are ANDed.
                                type: object
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                      type: array
                  type: object
                type: array
              ingressRule:
                items:
                  description: IngressRule and EgressRule are used to control the
                    wireflow's traffic flow.
                  properties:
                    from:
                      items:
                        properties:
                          ipBlock:
                            properties:
                              cidr:
                                type: string
                            type: object
                          peerSelector:
                            description: |-
                              A label selector is a label query over a set of resources. The result of matchLabels and
                              matchExpressions are ANDed. An empty label selector matches all objects. A null
                              label selector matches no objects.
                            properties:
                              matchExpressions:
                                description: matchExpressions is a list of label selector
                                  requirements. The requirements are ANDed.
                                items:
                                  description: |-
                                    A label selector requirement is a selector that contains values, a key, and an operator that
                                    relates the key and values.
                                  properties:
                                    key:
                                      description: key is the label key that the selector
                                        applies to.
                                      type: string
                                    operator:
                                      description: |-
                                        operator represents a key's relationship to a set of values.
                                        Valid operators are In, NotIn, Exists and DoesNotExist.
                                      type: string
                                    values:
                                      description: |-
                                        values is an array of string values. If the operator is In or NotIn,
                                        the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                        the values array must be empty. This array is replaced during a strategic
                                        merge patch.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  required:
                                  - key
                                  - operator
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              matchLabels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                  map is equivalent to an element of matchExpressions, whose key field is "key", the
                                  operator is "In", and the values array contains only "value". The requirements are ANDed.
                                type: object
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                      type: array
                    ports:
                      items:
                        properties:
                          port:
                            format: int32
                            type: integer
                          protocol:
                            type: string
                        type: object
                      type: array
                  type: object
                type: array
              nodeSelector:
                description: NodeSelector is a label query over node that should be
                  applied to the wireflow policy.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              policyType:
                type: string
            type: object
          status:
            description: NetworkPolicyStatus defines the observed state of NetworkPolicy.
            properties:
              ruleCount:
                description: 规则条数（Ingress + Egress）
                type: integer
              targetNodes:
                description: 策略当前匹配到的节点数量
                type: integer
            required:
            - ruleCount
            - targetNodes
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: networks.wireflowcontroller.wireflow.run
spec:
  group: wireflowcontroller.wireflow.run
  names:
    kind: Network
    listKind: NetworkList
    plural: networks
    shortNames:
    - wfnet
    - wfnetwork
    singular: network
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The current phase of the network
      jsonPath: .status.phase
      name: PHASE
      type: string
    - description: The CIDR block of the network
      jsonPath: .spec.cidr
      name: CIDR
      type: string
    - description: Number of nodes in the network
      jsonPath: .status.addedNodes
      name: NODES
      type: integer
    - description: Number of available IP addresses
      jsonPath: .status.availableIPs
      name: IPS-AVAIL
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Network is the Schema for the networks API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NetworkSpec defines the desired state of Network.
            properties:
              cidr:
                pattern: ^([0-9]{1,3}\.){3}[0-9]{1,3}/[0-9]{1,2}$
                type: string
              dns:
                properties:
                  enabled:
                    type: boolean
                  servers:
                    items:
                      type: string
                    type: array
                required:
                - enabled
                type: object
              mtu:
                type: integer
              name:
                description: name of network
                type: string
              networkId:
                type: string
              nodeSelector:
                additionalProperties:
                  type: string
                type: object
              nodes:
                items:
                  type: string
                type: array
              owner:
                type: string
              policies:
                items:
                  type: string
                type: array
            required:
            - cidr
            type: object
          status:
            description: NetworkStatus defines the observed state of Network.
            properties:
              activeCIDR:
                type: string
              addedNodes:
                description: 加入的节点数量
                type: integer
              allocatedIPs:
                description: 已分配的 IP 列表
                items:
                  description: IPAllocation IP 分配记录
                  properties:
                    allocatedAt:
                      format: date-time
                      type: string
                    ip:
                      type: string
                    node:
                      type: string
                  required:
                  - allocatedAt
                  - ip
                  - node
                  type: object
                type: array
              availableIPs:
                description: 可用 IP 数量
                type: integer
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              phase:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: nodes.wireflowcontroller.wireflow.run
spec:
  group: wireflowcontroller.wireflow.run
  names:
    kind: Node
    listKind: NodeList
    plural: nodes
    shortNames:
    - wfnode
    - wfn
    singular: node
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The current status of the node
      jsonPath: .status.status
      name: STATUS
      type: string
    - description: The current phase of the node
      jsonPath: .status.phase
      name: PHASE
      type: string
    - description: The IP address allocated to the node
      jsonPath: .status.allocatedAddress
      name: IP
      type: string
    - description: The network the node belongs to
      jsonPath: .spec.network
      name: NETWORK
      type: string
    - description: Number of active connections
      jsonPath: .status.connectionSummary.connected
      name: CONNECTED
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Node is the Schema for the nodes API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NodeSpec defines the desired state of Node.
            properties:
              allowedIPs:
                items:
                  type: string
                type: array
              appId:
                type: string
              dnsServers:
                items:
                  type: string
                type: array
              interfaceName:
                description: Interface for the node
                type: string
              mtu:
                type: integer
              network:
                type: string
              networkPolicies:
                items:
                  type: string
                type: array
              platform:
                description: platform which node runs on
                type: string
              privateKey:
                type: string
              publicKey:
                type: string
            type: object
          status:
            description: NodeStatus defines the observed state of Node.
            properties:
              activeKey:
                description: Active key
                type: string
              activeNetwork:
                description: Active networks, record the network the node joined
                type: string
              activeNetworkPolicies:
                items:
                  type: string
                type: array
              allocatedAddress:
                description: Allocated IP address, auto allocated by controller
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectionSummary:
                description: Connection summary
                properties:
                  connected:
                    type: integer
                  disconnected:
                    type: integer
                  total:
                    type: integer
                required:
                - connected
                - disconnected
                - total
                type: object
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                description: ObserveGeneration is the generation observed by the controller.
                format: int64
                type: integer
              phase:
                type: string
              status:
                description: Node status
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: wireflow-controller
  name: wireflow-controller-manager
  namespace: wireflow-system
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: wireflow-wireflow-controller-admin
  namespace: wireflow-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: wireflow-controller
  name: wireflow-leader-election-role
  namespace: wireflow-system
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: wireflow-manager-role
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - networkpolicies
  - networks
  - nodes
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - networkpolicies/finalizers
  - networks/finalizers
  - nodes/finalizers
  verbs:
  - update
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - networkpolicies/status
  - networks/status
  - nodes/status
  verbs:
  - get
  - patch
  - update
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: wireflow-metrics-auth-role
rules:
- apiGroups:
  - authentication.k8s.io
  resources:
  - tokenreviews
  verbs:
  - create
- apiGroups:
  - authorization.k8s.io
  resources:
  - subjectaccessreviews
  verbs:
  - create
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: wireflow-metrics-reader
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: wireflow-controller
  name: wireflow-network-admin-role
rules:
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - networks
  verbs:
  - '*'
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - networks/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: wireflow-controller
  name: wireflow-network-editor-role
rules:
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - networks
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - networks/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: wireflow-controller
  name: wireflow-network-viewer-role
rules:
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - networks
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - networks/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: wireflow-controller
  name: wireflow-networkpolicy-admin-role
rules:
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - networkpolicies
  verbs:
  - '*'
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - networkpolicies/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: wireflow-controller
  name: wireflow-networkpolicy-editor-role
rules:
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - networkpolicies
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - networkpolicies/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: wireflow-controller
  name: wireflow-networkpolicy-viewer-role
rules:
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - networkpolicies
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - networkpolicies/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: wireflow-controller
  name: wireflow-node-admin-role
rules:
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - nodes
  verbs:
  - '*'
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - nodes/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: wireflow-controller
  name: wireflow-node-editor-role
rules:
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - nodes
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - nodes/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: wireflow-controller
  name: wireflow-node-viewer-role
rules:
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - nodes
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - wireflowcontroller.wireflow.run
  resources:
  - nodes/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: wireflow-controller
  name: wireflow-leader-election-rolebinding
  namespace: wireflow-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: wireflow-leader-election-role
subjects:
- kind: ServiceAccount
  name: wireflow-controller-manager
  namespace: wireflow-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: wireflow-controller
  name: wireflow-manager-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: wireflow-manager-role
subjects:
- kind: ServiceAccount
  name: wireflow-controller-manager
  namespace: wireflow-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: wireflow-metrics-auth-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: wireflow-metrics-auth-role
subjects:
- kind: ServiceAccount
  name: wireflow-controller-manager
  namespace: wireflow-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: wireflow-wireflow-controller-admin
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
- kind: ServiceAccount
  name: wireflow-wireflow-controller-admin
  namespace: wireflow-system
---
apiVersion: v1
data:
  control.yaml: |-
    listen: :8080
    database:
      host: 81.68.109.143
      port: 3306
      name: linkany
      user: root
      password: dhvuYUoxnSZMavdJ
    Redis:
      Addr: 81.68.109.143:6379
      Password: dhvuYap
kind: ConfigMap
metadata:
  name: wireflow-control-config
  namespace: wireflow-system
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: wireflow-controller
    control-plane: controller-manager
  name: wireflow-controller-manager-metrics-service
  namespace: wireflow-system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    app.kubernetes.io/name: wireflow-controller
    control-plane: controller-manager
---
apiVersion: v1
kind: Service
metadata:
  name: wireflow-nats-service
  namespace: wireflow-system
spec:
  ports:
  - name: client
    port: 4222
    targetPort: 4222
  - name: monitor
    port: 8222
    targetPort: 8222
  selector:
    app: nats
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    k8s-app: wireflow-control
  name: wireflow-control
  namespace: wireflow-system
spec:
  replicas: 1
  selector:
    matchLabels:
      k8s-app: wireflow-control
  template:
    metadata:
      labels:
        k8s-app: wireflow-control
    spec:
      containers:
      - args:
        - start
        - manager
        - --log-level=INFO
        image: registry.cn-hangzhou.aliyuncs.com/wireflow-io/manager:dev
        imagePullPolicy: Always
        name: wireflow-control
        volumeMounts:
        - mountPath: /app/control.yaml
          name: conf
          subPath: control.yaml
      hostAliases:
      - hostnames:
        - signaling.wireflow.run
        ip: 81.68.109.143
      initContainers:
      - command:
        - /bin/sh
        - -c
        - |
          until curl -f http://wireflow-nats-service:8222/varz; do
            echo "Waiting for nats server..."
            sleep 2
          done
        image: curlimages/curl:latest
        name: wait-for-nats
      serviceAccountName: wireflow-wireflow-controller-admin
      volumes:
      - configMap:
          items:
          - key: control.yaml
            path: control.yaml
          name: wireflow-control-config
        name: conf
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: wireflow-controller
    control-plane: controller-manager
  name: wireflow-controller-manager
  namespace: wireflow-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: wireflow-controller
      control-plane: controller-manager
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        app.kubernetes.io/name: wireflow-controller
        control-plane: controller-manager
    spec:
      containers:
      - args:
        - --metrics-bind-address=:8443
        - start
        - controller
        - --leader-elect
        - --health-probe-bind-address=:8081
        image: registry.cn-hangzhou.aliyuncs.com/wireflow-io/manager:dev
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        name: manager
        ports: []
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
        volumeMounts: []
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: wireflow-controller-manager
      terminationGracePeriodSeconds: 10
      volumes: []
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wireflow-nats
  namespace: wireflow-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nats
  template:
    metadata:
      labels:
        app: nats
    spec:
      containers:
      - args:
        - -js
        - --sd
        - /data/jetstream
        - -m
        - "8222"
        image: nats:latest
        name: nats
        ports:
        - containerPort: 4222
          hostPort: 4222
        - containerPort: 8222
          hostPort: 8222
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    k8s-app: wireflow-turn
  name: wireflow-wireflow-turn
  namespace: wireflow-system
spec:
  replicas: 1
  selector:
    matchLabels:
      k8s-app: wireflow-turn
  template:
    metadata:
      labels:
        k8s-app: wireflow-turn
    spec:
      containers:
      - args:
        - start
        - turn
        - --log-level=verbose
        - --public-ip=81.68.109.143
        image: registry.cn-hangzhou.aliyuncs.com/wireflow-io/manager:dev
        imagePullPolicy: Always
        name: wireflow-turn
        ports:
        - containerPort: 3478
          hostPort: 3478
          protocol: UDP
      hostAliases:
      - hostnames:
        - signaling.wireflow.run
        ip: 81.68.109.143
      initContainers:
      - command:
        - /bin/sh
        - -c
        - |
          until curl -f http://wireflow-nats-service:8222/varz; do
            echo "Waiting for nats server..."
            sleep 2
          done
        image: curlimages/curl:latest
        name: wait-for-nats
